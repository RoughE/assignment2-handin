1. Add an "Ignore List", where anything listed will not be synced.
2. Add matcher that checks if files match exactly, that is, if they have the same names, sizes, and contents.
3. Add command line interface that asks for directories to use instead of taking them as arguments.